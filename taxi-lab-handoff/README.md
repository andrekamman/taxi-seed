# taxi-lab handoff

**The handoff is complete — this directory can be deleted.**

It held the k6 SQL load-testing design spec and implementation plan while
[`taxi-lab`](https://github.com/andrekamman/taxi-lab) was being bootstrapped. Both files
now live there, as `docs/specs/2026-03-25-k6-sql-load-testing-design.md` and
`docs/plans/2026-03-25-k6-sql-load-testing.md`, and the k6 load tester itself ships from
that repo.

The copies here are kept outside `docs/`, so they are not part of the published mkdocs
site.
